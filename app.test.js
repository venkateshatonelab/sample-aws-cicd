test('hello world', () => {
	expect('hello world').toBe('hello world');
});

test('success', () => {
	expect('success').toBe('success');
});

test('failed', () => {
	expect('failed').toBe('failed');
});
